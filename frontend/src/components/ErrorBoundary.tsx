import React from 'react'interface ErrorBoundaryState {  hasError: boolean  error: Error | null}class ErrorBoundary extends React.Component<React.PropsWithChildren<{}>, ErrorBoundaryState> {  constructor(props: React.PropsWithChildren<{}>) {    super(props)    this.state = { hasError: false, error: null }  }  static getDerivedStateFromError(error: Error): ErrorBoundaryState {    return { hasError: true, error }  }  componentDidCatch(error: Error, errorInfo: React.ErrorInfo): void {    console.error('ErrorBoundary caught an error', error, errorInfo)  }  render() {    if (this.state.hasError) {      return (        <div className="min-h-screen flex items-center justify-center bg-background p-6">          <div className="max-w-lg p-8 bg-white rounded-lg shadow-lg border border-secondary text-center">            <h2 className="text-2xl font-bold text-red-600 mb-3">Something went wrong</h2>            <p className="text-text mb-4">We encountered an unexpected issue. Please refresh, or come back later.</p>            <button              className="bg-primary text-white px-5 py-2 rounded-lg hover:bg-secondary transition-colors"              onClick={() => window.location.reload()}            >              Reload Page            </button>            {this.state.error && <pre className="text-xs text-left text-gray-500 mt-4 truncate">{this.state.error.message}</pre>}          </div>        </div>      )    }    return this.props.children  }}export default ErrorBoundary