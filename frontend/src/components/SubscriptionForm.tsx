import { useState, useEffect } from 'react'import type { Subscription } from '../types'interface SubscriptionFormProps {  subscription?: Subscription  onSubmit: (data: Omit<Subscription, '_id' | 'createdAt' | 'updatedAt'>) => void  onCancel: () => void  isLoading?: boolean}function SubscriptionForm({ subscription, onSubmit, onCancel, isLoading = false }: SubscriptionFormProps) {  const [formData, setFormData] = useState({    name: '',    frequency: 'monthly' as 'monthly' | 'yearly',    price: '',    nextPaymentDate: '',    category: 'general',    user: '507f1f77bcf86cd799439011', // Temporary user ID    reminderEnabled: true,    reminderDaysBefore: 7,  })  const [formError, setFormError] = useState('')  useEffect(() => {    if (subscription) {      setFormData({        name: subscription.name,        frequency: subscription.frequency,        price: subscription.price.toString(),        nextPaymentDate: subscription.nextPaymentDate.split('T')[0], // Format for date input        category: subscription.category,        user: subscription.user,        reminderEnabled: subscription.reminderEnabled ?? true,        reminderDaysBefore: subscription.reminderDaysBefore ?? 7,      })    }  }, [subscription])  const handleSubmit = (e: React.FormEvent) => {    e.preventDefault()    if (!formData.name.trim()) {      setFormError('Service name is required.')      return    }    const parsedPrice = parseFloat(formData.price)    if (Number.isNaN(parsedPrice) || parsedPrice <= 0) {      setFormError('Price must be a positive number.')      return    }    if (!formData.nextPaymentDate) {      setFormError('Next payment date is required.')      return    }    const nextDate = new Date(formData.nextPaymentDate)    if (nextDate <= new Date()) {      setFormError('Next payment date must be in the future.')      return    }    const reminderDays = Number(formData.reminderDaysBefore)    if (Number.isNaN(reminderDays) || reminderDays < 0) {      setFormError('Reminder days before must be a non-negative number.')      return    }    setFormError('')    onSubmit({      ...formData,      price: parsedPrice,      nextPaymentDate: new Date(formData.nextPaymentDate).toISOString(),      reminderEnabled: Boolean(formData.reminderEnabled),      reminderDaysBefore: reminderDays,    })  }  const handleChange = (e: React.ChangeEvent<HTMLInputElement | HTMLSelectElement>) => {    const { name, value } = e.target    setFormData(prev => ({ ...prev, [name]: value }))  }  return (    <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center p-4 z-50">      <div className="bg-white rounded-lg shadow-xl max-w-md w-full max-h-[90vh] overflow-y-auto">        <div className="p-6">          <h2 className="text-xl font-semibold mb-4">            {subscription ? 'Edit Subscription' : 'Add New Subscription'}          </h2>          {formError && (            <div className="mb-4 px-3 py-2 bg-red-100 text-red-700 rounded-md">              {formError}            </div>          )}          <form onSubmit={handleSubmit} className="space-y-4">            <div>              <label className="block text-sm font-medium text-text mb-1">                Service Name *              </label>              <input                type="text"                name="name"                value={formData.name}                onChange={handleChange}                required                className="w-full px-3 py-2 border border-secondary rounded-md focus:outline-none focus:ring-2 focus:ring-primary"                placeholder="e.g., Netflix, Spotify"              />            </div>            <div className="grid grid-cols-2 gap-4">              <div>                <label className="block text-sm font-medium text-text mb-1">                  Price *                </label>                <input                  type="number"                  name="price"                  value={formData.price}                  onChange={handleChange}                  required                  min="0"                  step="0.01"                  className="w-full px-3 py-2 border border-secondary rounded-md focus:outline-none focus:ring-2 focus:ring-primary"                  placeholder="0.00"                />              </div>              <div>                <label className="block text-sm font-medium text-text mb-1">                  Frequency                </label>                <select                  name="frequency"                  value={formData.frequency}                  onChange={handleChange}                  className="w-full px-3 py-2 border border-secondary rounded-md focus:outline-none focus:ring-2 focus:ring-primary"                >                  <option value="monthly">Monthly</option>                  <option value="yearly">Yearly</option>                </select>              </div>            </div>            <div>              <label className="block text-sm font-medium text-text mb-1">                Next Payment Date *              </label>              <input                type="date"                name="nextPaymentDate"                value={formData.nextPaymentDate}                onChange={handleChange}                required                className="w-full px-3 py-2 border border-secondary rounded-md focus:outline-none focus:ring-2 focus:ring-primary"              />            </div>            <div>              <label className="block text-sm font-medium text-text mb-1">                Category              </label>              <select                name="category"                value={formData.category}                onChange={handleChange}                className="w-full px-3 py-2 border border-secondary rounded-md focus:outline-none focus:ring-2 focus:ring-primary"              >                <option value="general">General</option>                <option value="entertainment">Entertainment</option>                <option value="productivity">Productivity</option>                <option value="health">Health & Fitness</option>                <option value="finance">Finance</option>                <option value="education">Education</option>                <option value="utilities">Utilities</option>                <option value="travel">Travel</option>                <option value="productivity">Productivity</option>                <option value="shopping">Shopping</option>              </select>            </div>            <div className="flex items-center justify-between gap-4">              <label className="flex items-center gap-2 text-text">                <input                  type="checkbox"                  name="reminderEnabled"                  checked={formData.reminderEnabled}                  onChange={(e) =>                    setFormData((prev) => ({ ...prev, reminderEnabled: e.target.checked }))                  }                  className="w-4 h-4"                />                Enable payment reminders              </label>              <div className="flex-1">                <label className="block text-sm font-medium text-text mb-1">Reminder days before</label>                <input                  type="number"                  name="reminderDaysBefore"                  value={formData.reminderDaysBefore}                  onChange={handleChange}                  min={0}                  className="w-full px-3 py-2 border border-secondary rounded-md focus:outline-none focus:ring-2 focus:ring-primary"                />              </div>            </div>            <div className="flex space-x-3 pt-4">              <button                type="button"                onClick={onCancel}                className="flex-1 px-4 py-2 text-text bg-background rounded-md hover:bg-secondary focus:outline-none focus:ring-2 focus:ring-secondary"                disabled={isLoading}              >                Cancel              </button>              <button                type="submit"                className="flex-1 px-4 py-2 text-white bg-primary rounded-md hover:bg-secondary focus:outline-none focus:ring-2 focus:ring-primary disabled:opacity-50"                disabled={isLoading}              >                {isLoading ? 'Saving...' : (subscription ? 'Update' : 'Add')}              </button>            </div>          </form>        </div>      </div>    </div>  )}export default SubscriptionForm